[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm run build"

[deploy]
startCommand = "node scripts/init-db.mjs && NODE_ENV=production node dist/index.js"
restartPolicyType = "ON_FAILURE"
