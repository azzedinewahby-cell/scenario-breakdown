[build]
builder = "NIXPACKS"
buildCommand = "npm install --legacy-peer-deps && npm run build"

[deploy]
startCommand = "node scripts/init-db.mjs && npm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
