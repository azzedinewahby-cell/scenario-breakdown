[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm run build"

[deploy]
startCommand = "node scripts/init-db.mjs && pnpm run start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
